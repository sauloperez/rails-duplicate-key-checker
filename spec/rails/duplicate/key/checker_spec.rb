require "spec_helper"

describe Rails::Duplicate::Key::Checker do
  it "has a version number" do
    expect(Rails::Duplicate::Key::Checker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
